#name: Build and Push Docker Image
#
#on:
#  push:
#    branches:
#      - main
#    paths-ignore:
#      - 'docs/**'
#      - '**.md'
#      - '.gitignore'
#      - 'README.md'
#
#jobs:
#
#  build-and-push:
#
#    runs-on: ubuntu-latest
#
#
#    steps:
#
#      - name: Checkout Repository
#        uses: actions/checkout@v4
#
#      - name: Setup Java 21
#        uses: actions/setup-java@v4
#        with:
#          distribution: temurin
#          java-version: 21
#
#      - name: Build Spring Boot App
#        run: mvn clean package -DskipTests
#
#
#      - name: Login to Docker Hub
#        uses: docker/login-action@v4
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build Docker Image
#        run: docker build -t zohaibsaraj/research-app:${{ github.sha }} -t zohaibsaraj/research-app:latest .
#
#      - name: Push Docker Image
#        run: |
#          docker push zohaibsaraj/research-app:${{ github.sha }}
#          docker push zohaibsaraj/research-app:latest
#
#
#
#
#
#
#
#
#
#
#
#
##name: Spring Boot CI/CD
##
##on:
##  push:
##    branches:
##      - main
##
##jobs:
##
##  build-and-push:
##
##    runs-on: ubuntu-latest
##
##    steps:
##
##      # Checkout Repository
##      - name: Checkout Repository
##        uses: actions/checkout@v6
##
##
##      # Setup Java
##      - name: Setup Java 17
##        uses: actions/setup-java@v4
##        with:
##          distribution: temurin
##          java-version: 17
##
##
##      # Build Spring Boot Project
##      - name: Build with Maven
##        run: mvn clean package -DskipTests
##
##
##      # Login to Docker Hub
##      - name: Login to Docker Hub
##        uses: docker/login-action@v3
##        with:
##          username: ${{ secrets.DOCKER_USERNAME }}
##          password: ${{ secrets.DOCKER_PASSWORD }}
##
##
##      # Build Docker Image
##      - name: Build Docker Image
##        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/research-app:v1 .
##
##      # Push Docker Image
##      - name: Push Docker Image
##        run: docker push ${{ secrets.DOCKER_USERNAME }}/research-app:v1
#
#
#
#
#
#
#
name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Build Spring Boot App
        run: mvn clean package -DskipTests

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Multi-Arch Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            zohaibsaraj/research-app:latest
            zohaibsaraj/research-app:${{ github.sha }}
          platforms: linux/amd64,linux/arm64